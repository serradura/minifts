# Update Log

## 2026-07-18
* **Rename**: Renamed the `compatibility/` folder to `fidelity/` to align with the README's *Fidelity* section and the bundle's fidelity vocabulary — the folder holds the cross-runtime interchange proof, which *is* the fidelity evidence. Updated every path reference (the `rake compat` task's `Dir.chdir`, the gemspec package-exclude list, `.rubocop.yml`, and the [interchange-suite](/porting/interchange-suite.md) `resource:`), and renamed the npm harness to `minifts-fidelity`. The `rake compat` task name is unchanged.
* **Rename**: Renamed the gem from `minisearch`/`Minisearch` to `minifts`/`MiniFTS` (Mini Full-Text Search). RubyGems' name-similarity policy rejects `minisearch` as too close to the pre-existing [`mini_search`](https://rubygems.org/gems/mini_search) gem (both normalize to the same name), so the package could not be published under the old name. The rename covers the gemspec (`minifts.gemspec`, `spec.name`, homepage `serradura/minifts`), the load path (`lib/minifts.rb`, `lib/minifts/`), the `MiniFTS` class and its `MiniFTS::Error`/`WILDCARD`/`VERSION`/`SearchableMap` constants, the `[minifts]` logger prefix and `MiniFTS:` error messages, the full test/benchmark/fidelity suites, and this bundle (files, titles, cross-links). Upstream references are deliberately **unchanged**: the JavaScript library is still `MiniSearch` ([lucaong/minisearch](https://github.com/lucaong/minisearch)), the interop target is still the `minisearch@7.2.0` npm package, and the reference source still lives at `tmp/minisearch/src/`. Public API, scores, and the byte-identical JSON index format are untouched — this is a name change only, `1.0.0` still passes 169 runs / 150,409 assertions.
* **Update**: Recorded the Ruby ⇄ JavaScript index-interchange work. Added a new [interchange-suite](/porting/interchange-suite.md) Reference — the bidirectional `rake compat` proof (32 scenarios against the real `minisearch@7.2.0`, 32/32 functional / 31/32 byte-identical), the two serializer gaps it surfaced, the astral-plane byte-order boundary, and the vacuum-before-materialize note. Catalogued the two new traps (whole-float number spelling, integer-key ordering) in [js-fidelity-gotchas](/porting/js-fidelity-gotchas.md); qualified the byte-identity claim with the astral exception and the bidirectional proof in [bit-for-bit-fidelity](/decisions/bit-for-bit-fidelity.md) and the iteration-order contract in [radix-tree-index](/architecture/radix-tree-index.md); cross-linked the [differential-oracle](/porting/differential-oracle.md). Note: the two serializer fixes and the `fidelity/` suite ship in this change; the fidelity claims now hold byte-for-byte for any corpus without astral-plane characters.

## 2026-07-17
* **Creation**: Seeded the bundle documenting the non-obvious knowledge behind the Ruby port — the design decisions, the runtime architecture, the JavaScript-fidelity porting notes, and the floor-respecting toolchain — alongside the 0.1.0 initial release.
  * Decisions — [ruby-2.4-floor](/decisions/ruby-2.4-floor.md), [bit-for-bit-fidelity](/decisions/bit-for-bit-fidelity.md), [minifts-role-in-okf](/decisions/minifts-role-in-okf.md).
  * Architecture — [search-engine](/architecture/search-engine.md), [radix-tree-index](/architecture/radix-tree-index.md), [bm25-scoring](/architecture/bm25-scoring.md).
  * Porting — [js-fidelity-gotchas](/porting/js-fidelity-gotchas.md), [differential-oracle](/porting/differential-oracle.md).
  * Toolchain — [ruby-floor-ci](/toolchain/ruby-floor-ci.md).
* **Update**: Recorded benchmark evidence for the okf role — `benchmarks/okf_vs_minifts.rb` measures ~44–56× minifts query throughput over okf's linear-scan search on the real `@okf` bundle — in [minifts-role-in-okf](/decisions/minifts-role-in-okf.md).
* **Update**: Promoted that evidence to a first-class concept — [okf-vs-minifts](/benchmarks/okf-vs-minifts.md) in a new `benchmarks/` area (the full method, results, and tradeoff) — and pointed the [okf-role decision](/decisions/minifts-role-in-okf.md) and the [search engine](/architecture/search-engine.md) at it.
* **Update**: Full-parity test expansion — added a `ranking.json` fixture (the reference's movie/song ranking sets) and a hand-ported parity suite covering every non-async reference case; the suite is now 169 runs / 150,409 assertions. Refreshed [differential-oracle](/porting/differential-oracle.md) (counts, new fixture) and dropped two out-of-bundle `README` cross-links that `validate` could not resolve.
* **Update**: Recorded the allocation-tuning campaign. Added a new [allocation-tuning](/benchmarks/allocation-tuning.md) Benchmark — the frozen-evaluator harness (`benchmarks/harness.rb`), the one-character-String-elimination pattern, the deterministic-vs-noisy metrics lesson, and the real-corpus before/after (indexing +37 % / 4.3× leaner, search +15 % / 1.8× leaner, byte-identical throughout). Noted the now-deliberate allocation idioms in [radix-tree-index](/architecture/radix-tree-index.md) (getbyte/codepoint hot paths) and [search-engine](/architecture/search-engine.md) (positional-Array result record), flagged [differential-oracle](/porting/differential-oracle.md) as the campaign's Gate 0, and marked the [okf-vs-minifts](/benchmarks/okf-vs-minifts.md) figures as pre-tuning.
